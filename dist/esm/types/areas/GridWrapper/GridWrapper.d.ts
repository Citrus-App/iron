import React from 'react';
declare type Props = {
    children?: React.ReactNode;
    full?: boolean;
};
declare const GridWrapper: ({ children, full }: Props) => JSX.Element;
export default GridWrapper;
