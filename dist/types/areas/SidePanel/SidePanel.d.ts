import React from 'react';
declare type Props = {
    children?: React.ReactNode;
    alignment?: string;
    state?: boolean;
    alignType?: string;
};
declare const SidePanel: ({ children, alignment, state, alignType }: Props) => JSX.Element;
export default SidePanel;
